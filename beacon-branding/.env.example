# Google Gemini API key (AI Studio key). Do NOT commit your real key.
NANO_BANANA_API_KEY=

# Default model to use when the client doesn't specify one.
# Recommended: gemini-2.5-flash-image
NANO_BANANA_MODEL_DEFAULT=gemini-2.5-flash-image

# Optional: server port (defaults to 8787)
NANO_BANANA_SERVER_PORT=8787
